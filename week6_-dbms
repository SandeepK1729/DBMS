POCEDURES : A stored procedure is a collection of sql statements that are stored in database. We can accept parameters and set variables, we can write if statements, loops, etc. within a stored procedure.
It is a sub program that performs action.

Syntax :
	create [or replace] procedure procedure_name(parameter variable_name datatype) is/as
	begin
	//code
	end;
























